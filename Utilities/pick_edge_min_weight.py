def pick_edge_min_weight(list_edges):
    
    weights = lambda(([edge], weight = edge[2]), list_edges)
    min_weight = lmin(weights)
        
    edge_min_weight= lambda([edge] edge[2] == min_weight, list_edges)
    
    return edge_min_weight